export interface Recommendation { userId: string; displayName: string | null; department: string | null; matchedTags: string[]; explanations: Array<'skill_overlap' | 'request_type_match' | 'availability' | 'recency'> }
export interface ProfileView { userId: string; displayName: string | null; username?: string | null; avatarUrl: string | null; course?: string | null; department?: string | null; studyYear?: number | null; graduationYear?: number | null; location?: string | null; availability?: Record<string, unknown>; profileVisibility?: 'campus' | 'connections' | 'private'; bio?: string | null; skills?: string[]; interests?: string[]; links?: { label: string; url: string }[]; discoverable: boolean; isSelf: boolean; isCrossCampus: boolean }
export interface TeamRequest { id: string; scope: 'campus' | 'global'; goalType: string | null; title: string; description: string | null; neededTags: string[]; timeWindowStart: string | null; timeWindowEnd: string | null; capacity: number | null; applicationPrompt: string | null; status: 'open' | 'closed'; version: number; ownerId: string; isOwner: boolean; myApplicationId: string | null; myApplicationState: 'pending' | 'accepted' | 'declined' | null; myApplicationKind: 'application' | 'invitation' | null; createdAt: string }
export interface TeamRequestPage { items: TeamRequest[]; nextCursor: string | null }
export interface TeamApplication { id: string; teamRequestId: string; applicantId: string; applicantDisplayName?: string | null; teamTitle?: string | null; responseText: string | null; kind: 'application' | 'invitation'; state: 'pending' | 'accepted' | 'declined' | 'withdrawn' | 'cancelled'; createdAt: string; respondedAt: string | null }
export interface ConnectionView { id: string; otherUserId: string; otherDisplayName: string | null; isCrossCampus: boolean; origin: string; direction: 'incoming' | 'outgoing'; state: 'pending' | 'accepted' | 'declined' | 'ended'; createdAt: string; acceptedAt: string | null; endedAt: string | null }
